[package]
name = "eclipse"
version = "0.1.0"
edition = "2024"

[workspace]
resolver = "2"
members = [
    "compiler",
    "compiler/lexer",
    "compiler/parser",
    "compiler/analyzer",

    "cli",
    "lsp",
    "common",
    "diagnostics",
]

# default-members = ["compiler", "cli"]

[dependencies]
