[package]
name = "analyzer"
version = "0.1.0"
edition = "2024"

[dependencies]
syntax = { path = "../syntax" }
context = { path = "../context" }
resolver = { path = "../resolver" }
common = { path = "../../common" }
diagnostics = { path = "../diagnostics" }
