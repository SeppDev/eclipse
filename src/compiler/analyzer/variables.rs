use std::collections::{HashMap, HashSet};

pub struct Variable {
    key: String
}

pub struct Variables {
    variables: HashMap<>,
    keys: HashSet<>
}
impl Variables {
    pub fn insert(&mut self, key: String) {
        
    }
}