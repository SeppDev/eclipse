use crate::compiler::{parser::ExpressionInfo, types::Type};

use super::{FunctionCtx, ProgramCtx};

pub fn handle_expression(program: &mut ProgramCtx, function: &mut FunctionCtx, infered_type: Option<Type>, expression: Option<ExpressionInfo>) {
    
}
