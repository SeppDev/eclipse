use crate::{
    lexer::{Token, TokensGroup},
    CompileError, ParseResult,
};

use super::parser::expect_tokens;

pub fn parse_enum(tokens: &mut TokensGroup) -> ParseResult<Vec<String>> {


    return Ok(generics);
}
