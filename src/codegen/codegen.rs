use crate::{
    analyzer::{FunctionTypes, IRNode},
    codegen::builder::Builder,
    Path,
};

use super::builder::ScopeBuilder;

pub fn generate() {

    // let mut builder = Builder::new();

    // println!("{:#?}", builder);
}
